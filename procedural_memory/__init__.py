"""
procedural_memory — ARBOR 의 절차 기억 (실행 본체).

DSL 의 *실행 본체* (property/util/transformation) 가 여기 산다.
각 DSL 의 *선언적 명세* 는 DSL.registry.SPECS 에 함께 등록된다 (= semantic
DSL 라이브러리의 씨앗, Slice 1 은 (ㄱ) 개념만 — 물리 분리는 Slice 2).
"""
